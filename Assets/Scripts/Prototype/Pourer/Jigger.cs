using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/// <summary>
/// 지거 관리 컴포넌트
/// </summary>
public class Jigger : MonoBehaviour
{
    //Glass 배끼기
    //내용물이 있으면 다른 내용물 못넣음 (믹싱 플로팅 등등 불가)
    //채울때는 Glass와 동일하게
    //비울때는 한번에

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
